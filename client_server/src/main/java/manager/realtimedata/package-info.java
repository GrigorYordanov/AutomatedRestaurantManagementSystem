/**
 * realtimedata contains the controller, main and object classes that are used
 * to fill the FXML table with data, so the manager can view real-time data data
 * of the restaurant.
 */

package manager.realtimedata;